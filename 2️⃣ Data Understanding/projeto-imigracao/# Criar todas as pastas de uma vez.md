# Criar todas as pastas de uma vez
mkdir -p data/raw/bpstat data/processed data/documentation notebooks/exploracao src docs/logs
# Inicializar repositório Git
git init

# Configurar seu usuário (substitua com seus dados)
git config user.name "waltrickme"
git config user.email "waltrickme@gmail.com"
